<%= form_with(model: servercenter, local: true) do |form| %>
  <% if servercenter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(servercenter.errors.count, "error") %> prohibited this servercenter from being saved:</h2>

      <ul>
        <% servercenter.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :server_available %>
    <%= form.number_field :server_available %>
  </div>

  <div class="field">
    <%= form.label :servers_capacity %>
    <%= form.text_area :servers_capacity %>
  </div>

  <div class="field">
    <%= form.label :country_id %>
   <td><%= form.collection_select :country_id, Country.all, :id, :name %></td>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
